# render.yaml
services:
  - type: web
    name: product-recommender
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    pythonVersion: 3.10